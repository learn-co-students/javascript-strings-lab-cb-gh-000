/* Examples of: global concatenatedString, describe, interpolatedString, it,
   and myString in Javascript. */

// Declare the variable 'greeting'.
var greeting = "Hello, everybody!";

// Declare the variable 'specialGuest'.
var specialGuest = "Neil deGrasse Tyson"

// Declare the variable 'greetSpecialGuest' to equal
// "Hello, " followed by the variable 'specialGuest',
// ending with an exclamation mark.
var greetSpecialGuest = "Hello, " + specialGuest + "!";

// Declare the variable 'topic'.
var topic = "space";

// Declare the variable 'conversation', using opening single quote.
var conversation = `Let's talk about ${topic}.`;
